export class Track {
  constructor({ id, name, images, artists, totalTracks }) {
    this.id = id
    this.name = name
    this.images = images
    this.artists = artists
    this.totalTracks = totalTracks
  }
}
